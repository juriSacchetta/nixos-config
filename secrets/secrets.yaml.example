# SOPS secrets template
# Copy this to secrets.yaml and encrypt with: sops -e -i secrets.yaml
# You need to generate an age key first: age-keygen -o ~/.config/sops/age/keys.txt

# Example structure (replace with your actual encrypted values):
github_token: your_github_token_here
